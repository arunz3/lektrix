## 2025-05-22 - Missing ARIA labels on Icon-only buttons
**Learning:** Found several core UI components missing ARIA labels for their icon-only interactions, particularly modals and specific tool pages. Ensuring screen readers have adequate context for single-icon buttons makes the UX more inclusive.
**Action:** Always verify that every interactive element using only an icon provides an `aria-label`.
