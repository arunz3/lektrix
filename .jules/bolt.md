## 2025-05-29 - Image to PDF Optimization\n**Learning:** The `ImageToPDFTool` was sequentially awaiting `file.arrayBuffer()` and `embedJpg` / `embedPng` within a `for...of` loop, blocking parallel I/O and embedding operations.\n**Action:** Use `Promise.all()` with `map` to perform these independent asynchronous tasks concurrently, keeping the same page addition sequence to preserve correctness.
